# 项目领域词汇

## 产品定位

**本机智能资料管家**：帮助个人整理本机上的文件、文件夹、照片和视频。系统在本机分析信息，给出分类建议，由用户确认后改变项目所在位置，并保留可撤销记录。

## 核心术语

### 项目（Item）

用户可以被扫描、分类或移动的最小单位。项目可以是普通文件、普通文件夹、照片、视频，或由一张照片和一个视频组成的 Live Photo。

### 分类（Classification）

为项目确定一个业务类别或媒体归档字段。分类可以来自用户规则、用户手动选择或 AI 建议。分类本身不改变文件位置。

### 标签（Label）

用户可见的分类名称，例如“工作”“学习”“发票”。标签是分类结果的显示名称，不等同于文件系统目录。

### 目标位置（Destination）

根据项目分类和整理模板计算出的新路径。目标位置在移动发生前可以预览和修改。

默认情况下，分类路径就是目标目录路径；收件箱可以为分类配置不同的自定义目标目录，以适配已有目录结构。

### 整理（Organization）

按照用户确认的目标位置实际改变项目所在位置，并记录这次操作。整理不是给文件写入元数据，也不是复制一份文件。

### 批次（Batch）

一次用户确认后提交的一组整理操作。批次拥有独立的操作记录，可以整体查看和撤销。

### 建议（Recommendation）

系统根据项目名称、类型、样本或媒体元数据生成的候选分类。建议不代表已经移动，也不代表一定正确。

### 待确认项目（Review Item）

分类、日期、地点或目标位置仍不足以安全整理，需要用户检查或修改的项目。

### 整理规则（Organization Rule）

用户明确配置的匹配条件和目标位置模板。规则优先级高于 AI 建议，并用于重复、可预测的整理任务。

规则属于具体收件箱；不同收件箱可以使用不同的分类体系和目标位置模板。

建议冲突时，优先级依次为用户当前手动修改、明确整理规则、用户历史选择、媒体元数据规则、AI 建议和兜底分类。

系统可以从用户在同一收件箱中的重复手动修正中提取模式，但学习结果先作为规则建议展示，用户确认后才成为整理规则。学习信号不跨收件箱共享。

规则建议必须由用户明确确认后才生效。新项目的移动建议进入待处理列表，用户可以逐项修改，也可以批量确认一个或多个项目。

待处理列表跨应用重启保留。每个待处理项目绑定扫描时的文件身份；源文件被删除、移动或替换时，建议失效并标记为源文件已变化，不能直接执行旧建议。

### 收件箱分类体系（Inbox Taxonomy）

某个收件箱允许使用的分类集合及其显示名称。分类体系独立于其他收件箱，AI 建议必须从当前收件箱的分类体系中选择。

分类可以是扁平的，也可以由最多三级节点组成。分类节点的完整路径是分类身份的一部分，例如“项目/前端”与“项目/后端”是两个不同分类。

### 收件箱（Inbox）

用户指定的、持续接收未整理项目的目录，例如 Downloads 或桌面。收件箱中的项目先进入待处理状态，再根据规则或建议整理。

收件箱中新出现的项目默认只生成分类建议，并询问用户是否移动；项目不会因为被监控而自动改变位置。

### 本机分析（Local Analysis）

在用户设备上读取文件系统信息和媒体元数据。除用户明确允许的外部地点解析或 AI 分类输入外，媒体内容和路径不离开本机。
